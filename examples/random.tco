/*fn incr(a: &mut Int) {
    *a = *a + 1
}

fn main() {
    let mut a = 1
    print(a)
    incr(&mut a)
    print(a)
}*/

/*let mut a = 1
print(a)*/

//main()

// cascade
/*
let d = 1
    <- add(1)
    <- add(1)
    <- yourself
*/

// pipeline
/*
let player =
  Entity()
  |> add(Position(10, 5))
  |> add(Health(100, 100))
  |> yourself
*/

/*let mut a = 1
a += 1
print(a)*/

fn incr(a: &mut Int) {
    *a += 1
}
let mut a = 1
incr(&mut a)
print(a)
